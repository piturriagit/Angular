import { DateExpired } from './date-expired';

describe('DateExpired', () => {
  it('should create an instance', () => {
    const directive = new DateExpired();
    expect(directive).toBeTruthy();
  });
});
